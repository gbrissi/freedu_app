import 'package:flutter/material.dart';

class PostAnswersDeclaration extends StatelessWidget {
  const PostAnswersDeclaration({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Respostas",
      style: TextStyle(
        fontSize: 20,
        fontWeight: FontWeight.w600,
      ),
    );
  }
}
