import 'package:flutter/material.dart';

class AlternativeLoginDescription extends StatelessWidget {
  const AlternativeLoginDescription({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "ou entrar com",
      style: TextStyle(
        fontSize: 16,
      ),
    );
  }
}
